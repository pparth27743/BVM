/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/info",
  output: "standalone",
  compress: true,
  poweredByHeader: false,
  reactStrictMode: true,
};

export default nextConfig;
